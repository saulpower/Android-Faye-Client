package main.java.com.moneydesktop.finance.database;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import main.java.com.moneydesktop.finance.database.Institution;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/**
 * DAO for table INSTITUTION.
*/
public class InstitutionDao extends AbstractDao<Institution, Long> {

    public static final String TABLENAME = "INSTITUTION";

    /**
     * Properties of entity Institution.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CreatedOn = new Property(1, java.util.Date.class, "createdOn", false, "CREATED_ON");
        public final static Property InstitutionId = new Property(2, String.class, "institutionId", false, "INSTITUTION_ID");
        public final static Property Instructions = new Property(3, String.class, "instructions", false, "INSTRUCTIONS");
        public final static Property Name = new Property(4, String.class, "name", false, "NAME");
        public final static Property Phone = new Property(5, String.class, "phone", false, "PHONE");
        public final static Property Popularity = new Property(6, Integer.class, "popularity", false, "POPULARITY");
        public final static Property Status = new Property(7, Integer.class, "status", false, "STATUS");
        public final static Property UpdatedOn = new Property(8, java.util.Date.class, "updatedOn", false, "UPDATED_ON");
        public final static Property Url = new Property(9, String.class, "url", false, "URL");
        public final static Property BusinessObjectId = new Property(10, long.class, "businessObjectId", false, "BUSINESS_OBJECT_ID");
    };

    private DaoSession daoSession;


    public InstitutionDao(DaoConfig config) {
        super(config);
    }

    public InstitutionDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'INSTITUTION' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'CREATED_ON' INTEGER," + // 1: createdOn
                "'INSTITUTION_ID' TEXT," + // 2: institutionId
                "'INSTRUCTIONS' TEXT," + // 3: instructions
                "'NAME' TEXT," + // 4: name
                "'PHONE' TEXT," + // 5: phone
                "'POPULARITY' INTEGER," + // 6: popularity
                "'STATUS' INTEGER," + // 7: status
                "'UPDATED_ON' INTEGER," + // 8: updatedOn
                "'URL' TEXT," + // 9: url
                "'BUSINESS_OBJECT_ID' INTEGER NOT NULL );"); // 10: businessObjectId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'INSTITUTION'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Institution entity) {
        stmt.clearBindings();

        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }

        java.util.Date createdOn = entity.getCreatedOn();
        if (createdOn != null) {
            stmt.bindLong(2, createdOn.getTime());
        }

        String institutionId = entity.getInstitutionId();
        if (institutionId != null) {
            stmt.bindString(3, institutionId);
        }

        String instructions = entity.getInstructions();
        if (instructions != null) {
            stmt.bindString(4, instructions);
        }

        String name = entity.getName();
        if (name != null) {
            stmt.bindString(5, name);
        }

        String phone = entity.getPhone();
        if (phone != null) {
            stmt.bindString(6, phone);
        }

        Integer popularity = entity.getPopularity();
        if (popularity != null) {
            stmt.bindLong(7, popularity);
        }

        Integer status = entity.getStatus();
        if (status != null) {
            stmt.bindLong(8, status);
        }

        java.util.Date updatedOn = entity.getUpdatedOn();
        if (updatedOn != null) {
            stmt.bindLong(9, updatedOn.getTime());
        }

        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(10, url);
        }
        stmt.bindLong(11, entity.getBusinessObjectId());
    }

    @Override
    protected void attachEntity(Institution entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }

    /** @inheritdoc */
    @Override
    public Institution readEntity(Cursor cursor, int offset) {
        Institution entity = new Institution( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : new java.util.Date(cursor.getLong(offset + 1)), // createdOn
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // institutionId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // instructions
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // name
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // phone
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // popularity
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // status
            cursor.isNull(offset + 8) ? null : new java.util.Date(cursor.getLong(offset + 8)), // updatedOn
            cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9), // url
            cursor.getLong(offset + 10) // businessObjectId
        );
        return entity;
    }

    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Institution entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCreatedOn(cursor.isNull(offset + 1) ? null : new java.util.Date(cursor.getLong(offset + 1)));
        entity.setInstitutionId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setInstructions(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setPhone(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setPopularity(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setStatus(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setUpdatedOn(cursor.isNull(offset + 8) ? null : new java.util.Date(cursor.getLong(offset + 8)));
        entity.setUrl(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
        entity.setBusinessObjectId(cursor.getLong(offset + 10));
     }

    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Institution entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }

    /** @inheritdoc */
    @Override
    public Long getKey(Institution entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override
    protected boolean isEntityUpdateable() {
        return true;
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getBusinessObjectBaseDao().getAllColumns());
            builder.append(" FROM INSTITUTION T");
            builder.append(" LEFT JOIN BUSINESS_OBJECT_BASE T0 ON T.'BUSINESS_OBJECT_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }

    protected Institution loadCurrentDeep(Cursor cursor, boolean lock) {
        Institution entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        BusinessObjectBase businessObjectBase = loadCurrentOther(daoSession.getBusinessObjectBaseDao(), cursor, offset);
         if(businessObjectBase != null) {
            entity.setBusinessObjectBase(businessObjectBase);
        }

        return entity;
    }

    public Institution loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();

        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);

        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }

    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Institution> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Institution> list = new ArrayList<Institution>(count);

        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }

    protected List<Institution> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }


    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Institution> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }

}
