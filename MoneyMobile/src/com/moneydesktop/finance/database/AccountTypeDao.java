package com.moneydesktop.finance.database;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.moneydesktop.finance.database.AccountType;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ACCOUNT_TYPE.
*/
public class AccountTypeDao extends AbstractDao<AccountType, Long> {

    public static final String TABLENAME = "ACCOUNT_TYPE";

    /**
     * Properties of entity AccountType.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property AccountTypeId = new Property(1, String.class, "accountTypeId", false, "ACCOUNT_TYPE_ID");
        public final static Property AccountTypeName = new Property(2, String.class, "accountTypeName", false, "ACCOUNT_TYPE_NAME");
        public final static Property AggregationType = new Property(3, Integer.class, "aggregationType", false, "AGGREGATION_TYPE");
        public final static Property FinancialAccountType = new Property(4, Integer.class, "financialAccountType", false, "FINANCIAL_ACCOUNT_TYPE");
        public final static Property GroupKey = new Property(5, String.class, "groupKey", false, "GROUP_KEY");
        public final static Property BusinessObjectId = new Property(6, long.class, "businessObjectId", false, "BUSINESS_OBJECT_ID");
        public final static Property AccountTypeGroupId = new Property(7, Long.class, "accountTypeGroupId", false, "ACCOUNT_TYPE_GROUP_ID");
        public final static Property ParentAccountTypeId = new Property(8, Long.class, "parentAccountTypeId", false, "PARENT_ACCOUNT_TYPE_ID");
    };

    private DaoSession daoSession;

    private Query<AccountType> accountTypeGroup_AccountTypesQuery;
    private Query<AccountType> accountType_ChildrenQuery;

    public AccountTypeDao(DaoConfig config) {
        super(config);
    }
    
    public AccountTypeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ACCOUNT_TYPE' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'ACCOUNT_TYPE_ID' TEXT," + // 1: accountTypeId
                "'ACCOUNT_TYPE_NAME' TEXT," + // 2: accountTypeName
                "'AGGREGATION_TYPE' INTEGER," + // 3: aggregationType
                "'FINANCIAL_ACCOUNT_TYPE' INTEGER," + // 4: financialAccountType
                "'GROUP_KEY' TEXT," + // 5: groupKey
                "'BUSINESS_OBJECT_ID' INTEGER NOT NULL ," + // 6: businessObjectId
                "'ACCOUNT_TYPE_GROUP_ID' INTEGER," + // 7: accountTypeGroupId
                "'PARENT_ACCOUNT_TYPE_ID' INTEGER);"); // 8: parentAccountTypeId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ACCOUNT_TYPE'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, AccountType entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String accountTypeId = entity.getAccountTypeId();
        if (accountTypeId != null) {
            stmt.bindString(2, accountTypeId);
        }
 
        String accountTypeName = entity.getAccountTypeName();
        if (accountTypeName != null) {
            stmt.bindString(3, accountTypeName);
        }
 
        Integer aggregationType = entity.getAggregationType();
        if (aggregationType != null) {
            stmt.bindLong(4, aggregationType);
        }
 
        Integer financialAccountType = entity.getFinancialAccountType();
        if (financialAccountType != null) {
            stmt.bindLong(5, financialAccountType);
        }
 
        String groupKey = entity.getGroupKey();
        if (groupKey != null) {
            stmt.bindString(6, groupKey);
        }
        stmt.bindLong(7, entity.getBusinessObjectId());
 
        Long accountTypeGroupId = entity.getAccountTypeGroupId();
        if (accountTypeGroupId != null) {
            stmt.bindLong(8, accountTypeGroupId);
        }
 
        Long parentAccountTypeId = entity.getParentAccountTypeId();
        if (parentAccountTypeId != null) {
            stmt.bindLong(9, parentAccountTypeId);
        }
    }

    @Override
    protected void attachEntity(AccountType entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public AccountType readEntity(Cursor cursor, int offset) {
        AccountType entity = new AccountType( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // accountTypeId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // accountTypeName
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // aggregationType
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // financialAccountType
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // groupKey
            cursor.getLong(offset + 6), // businessObjectId
            cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7), // accountTypeGroupId
            cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8) // parentAccountTypeId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, AccountType entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAccountTypeId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAccountTypeName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setAggregationType(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setFinancialAccountType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setGroupKey(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setBusinessObjectId(cursor.getLong(offset + 6));
        entity.setAccountTypeGroupId(cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7));
        entity.setParentAccountTypeId(cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(AccountType entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(AccountType entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "accountTypes" to-many relationship of AccountTypeGroup. */
    public List<AccountType> _queryAccountTypeGroup_AccountTypes(Long accountTypeGroupId) {
        synchronized (this) {
            if (accountTypeGroup_AccountTypesQuery == null) {
                QueryBuilder<AccountType> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.AccountTypeGroupId.eq(null));
                accountTypeGroup_AccountTypesQuery = queryBuilder.build();
            }
        }
        Query<AccountType> query = accountTypeGroup_AccountTypesQuery.forCurrentThread();
        query.setParameter(0, accountTypeGroupId);
        return query.list();
    }

    /** Internal query to resolve the "children" to-many relationship of AccountType. */
    public List<AccountType> _queryAccountType_Children(Long parentAccountTypeId) {
        synchronized (this) {
            if (accountType_ChildrenQuery == null) {
                QueryBuilder<AccountType> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ParentAccountTypeId.eq(null));
                accountType_ChildrenQuery = queryBuilder.build();
            }
        }
        Query<AccountType> query = accountType_ChildrenQuery.forCurrentThread();
        query.setParameter(0, parentAccountTypeId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getBusinessObjectBaseDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getAccountTypeGroupDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getAccountTypeDao().getAllColumns());
            builder.append(" FROM ACCOUNT_TYPE T");
            builder.append(" LEFT JOIN BUSINESS_OBJECT_BASE T0 ON T.'BUSINESS_OBJECT_ID'=T0.'_id'");
            builder.append(" LEFT JOIN ACCOUNT_TYPE_GROUP T1 ON T.'ACCOUNT_TYPE_GROUP_ID'=T1.'_id'");
            builder.append(" LEFT JOIN ACCOUNT_TYPE T2 ON T.'PARENT_ACCOUNT_TYPE_ID'=T2.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected AccountType loadCurrentDeep(Cursor cursor, boolean lock) {
        AccountType entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        BusinessObjectBase businessObjectBase = loadCurrentOther(daoSession.getBusinessObjectBaseDao(), cursor, offset);
         if(businessObjectBase != null) {
            entity.setBusinessObjectBase(businessObjectBase);
        }
        offset += daoSession.getBusinessObjectBaseDao().getAllColumns().length;

        AccountTypeGroup accountTypeGroup = loadCurrentOther(daoSession.getAccountTypeGroupDao(), cursor, offset);
        entity.setAccountTypeGroup(accountTypeGroup);
        offset += daoSession.getAccountTypeGroupDao().getAllColumns().length;

        AccountType parent = loadCurrentOther(daoSession.getAccountTypeDao(), cursor, offset);
        entity.setParent(parent);

        return entity;    
    }

    public AccountType loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<AccountType> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<AccountType> list = new ArrayList<AccountType>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<AccountType> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<AccountType> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
